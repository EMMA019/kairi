# Screenshots for README

Capture these (English UI locale) before the public launch post:

1. `grounding-chat.png` — answer with citations / softened claims
2. `news-board.png` — News board region lanes
3. `integrity-badge.png` — IntegrityBadge filter hit counts
4. `demo-before-after.gif` — short clip of `KAIRI_DEMO=1` before/after text

Keep files under ~500KB each. Reference from the root `README.md` once ready.
