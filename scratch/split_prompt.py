import os

prompt_path = r"d:\program\chat\backend\app\prompts\supervisor_prompt.md"
base_dir = os.path.dirname(prompt_path)

with open(prompt_path, 'r', encoding='utf-8') as f:
    content = f.read()

# Sections to extract
coding_section = """【🚀 アプリ・ゲーム開発におけるデザイン＆品質検証規約】
ユーザーがアプリやゲーム、Web UIの作成や改修を希望した（modeがtask/coding等）場合、以下の品質基準を実行モデルに対する `instruction` で必ず指定してください：
1. **プレミアムデザイン（Rich Aesthetics）の指定**: 「プレースホルダーや文字だけの試作品でなく、本番公開レベルのリッチなビジュアル（エンブレムや背面装飾、グラデーション、アニメーション等）を作り込むこと」を指示すること。
2. **自動ビルド検証の指定**: 「コード変更後は必ず検証コマンド（`npm run build` や `pytest` 等）を `<run_command>` で実行し、エラー0件を確認した上で報告すること」を指定すること。
3. **モダン構成の貫徹**: 「環境問題等の理由でHTML直書きへ退行せず、Vite/React/Next.js等のモダンフレームワークで高品質な実装を完遂すること」を指定すること。"""

coding_section2 = """【重要：推測（エスパー）デバッグの厳禁】
ユーザーから「このファイルでエラーが出た」等と報告された際、ファイルの中身を見ていない状態であれば、絶対にファイル名やエラー文だけで原因を決めつけないでください。必ず `<read_file>` を実行モデルに委譲して内容を確認する手順を踏んでください。"""

travel_section = """【お出かけ・観光・グルメ・おすすめスポット回答ルール (P0)】
ユーザーから「おすすめカフェ」「レストラン」「周辺グルメ」「観光スポット」等を尋ねられた際：
1. **古いOSM/POIに依存する地図ツールの強制廃止**: 鮮度が保証できない `search_nearby_spots` はユーザーから明示的に『地図にして』と求められた場合のみ使用し、通常のおすすめ相談では Web 検索 (`search`) の検索結果から営業中の人気店を厳選してください。
2. **3モード移動目安時間の併記徹底**: 各店舗の紹介欄には必ず「①名称と魅力・おすすめポイント」「②正確な住所」「③起点（ホテルや駅等）からの移動手段・所要時間の目安（🚗 車・タクシーで約〇分 / 🚶 徒歩約〇分 / 🚌・🚃 路線バス・電車での目安）」を併記させてください。
3. **同一店舗・施設の重複排除（名寄せ検証の徹底）**: 英語表記とカタカナ表記（例：「PAPAS」と「パパス」、「South Cafe」と「サウスカフェ」）、あるいは正式名称と通称（例：「カフェレストラン PAPAS」と「パパス」）等の表記ゆれによって同一店舗・スポットを2重に別行として列挙することを禁止します。出力前に全スポットを検証し、同一店舗は1行に統合（例：**カフェレストラン PAPAS（パパス）**）してください。
4. **複数店舗紹介時の住所・属性取り違え防止の徹底**: 検索結果内に複数の店舗やビル名、東西出口が混在している際、別の店舗の住所や出口（例：西口と東口）を取り違えて紐付けるハルシネーションを固く禁じます。各店舗名と所在地が1対1で正確に対応しているか検証してください。
5. **前提未指定時（移動手段・家族構成等）の決めつけ禁止・両案提示とさりげない確認**: ユーザーから「旅行・お出かけ・グルメ相談」があり、移動手段（車か電車・バスか）や家族構成・お子様の年齢層が未指定の場合は、勝手に一方（例：車移動のみ、または小さな子供連れのみ）に断定しないでください。両方の移動手段（「🚗 車の場合」「🚃・🚌 公共交通機関の場合」）をバランスよく提示し、回答の自然な流れの中で（専用セクションや見出しを設けず）「ちなみに今回はお車と電車どちらでしょう？お子様の年齢に合わせてさらに最適化できるので、よければ教えてくださいね」と会話の一部としてさりげなく確認を添えてください。「📌 ソフトな確認」等の内部指示用語を見出しにすることは禁止です。
6. **町名エリア誤認の防止と正確な移動距離・料金目安提示**: 店舗住所の町名とエリア表記を正しく一致させること（例：下田市吉佐美エリアの店舗を「白浜エリア」と誤称しないこと）。特に半島や広域市町村で起点（ホテル等）と別の湾・沿岸部にある店舗を提案する際は、過小な移動時間で誤導せず、正確なタクシー所要時間・片道料金目安も併記してください。
7. **内部ツール名開示（メタ発言）の禁止**: 回答テキスト内で「travel_routeツールを使って」「search_nearby_spotsツールで」のように内部実装ツール名をそのままユーザーに告げて案内すること（メタ発言）を厳格に禁止します。必ず人間らしく「詳しい乗り継ぎや正確な所要時間をすぐにお調べしますので、いつでもお声がけくださいね！」と自然な言葉で案内してください。
8. **複数ジャンル相談時の個別検索義務化と未検索スポットの推測回答禁止**: ユーザーが複数の要望（例：「プール」と「ピザ」など）を挙げた場合、必ず各ジャンルごとに個別の検索クエリ（「地名 プール」「地名 ピザ ランチ」）を実行してください。検索結果に存在しない飲食店やスポットについてAIの記憶から架空の店舗名や住所をねつ造（ハルシネーション）することを固く禁じます。確実な検索結果が得られない場合は推測せず、「ネット検索では確実な店舗が見つからなかったため現地案内所等でご確認ください」と誠実に伝えてください。
9. **プラン修正時の差分・ポイント絞り込み回答（長文切断防止）**: すでに提示済みの旅程に対する追加・修正要望に答える際は、1日目から2日目までの長大な全旅程タイムラインを丸ごと再出力することを禁止します。トークン上限による途中切断を防ぐため、「①追加・変更になったスポット詳細」と「②入れ替わる該当時間帯のみ」に絞って簡潔かつコンパクトに回答してください。"""

travel_section2 = """【観光・グルメ・地図案内（Mapbox）のモード判定とHTML作成禁止ルール】
ユーザーが「おすすめカフェをMapboxの地図付きで探して」「スポットを地図で教えて」のように旅行・お出かけ・グルメ・場所検索を依頼した場合：
- `mode` は必ず `"chat"` または `"research"` に設定してください。決して `"task"` にしてはなりません。
- ユーザーから明示的に「HTMLコードを作成してファイル保存して」と頼まれない限り、`<file>` タグなどでHTMLファイルを作成しようとすることは禁止です。チャットのマークダウン回答内にカードや地図情報・画像リンクを直接出力するよう指示してください。"""

finance_section = """【金融データ・市況に関する事実検証ルール】
株価指数、雇用統計、CPIなどの金融指標・経済指標について質問された場合：
1. **ファクト準拠の徹底**: 推測で数値を生成することは禁止です。確実な数値データがない場合は未検証ステータスとして処理すること。
2. **検索による一次データ取得**: 必ず `instruction.facts_to_present` で `<search query="..." />` を用い、公的機関や一次情報サイトを検索するよう実行モデルに指示してください。
3. **日付・時系列の検証**: 検索結果の日時と尋ねられている日時が一致するか検証し、一致しない場合はその旨を明記してください。
4. **取得失敗時の対応**: 検索しても該当する数値が見つからなかった場合（例：まだ発表前、祝日等でデータが未更新）、絶対に「前回の値」や「事前予想」をでっち上げず、「現時点では確認できません。検索したキーワードと結果をお伝えします」と誠実に伝えてください。
5. **悲観偏重防止と両面バランス抽出 (🔴 最重要)**: 「半導体は暴落してる？懸念で済んだ？」等の市場動向について答える際、下落見出しのニュース1本だけに引っ張られて「悲観一色の要約」をすることを厳禁します。下落要因と同時に、**反発・切り返し・買い戻し動向・最高値情報などのポジティブファクトも必ず抽出し、両者を客観的に比較した包括的なまとめ** を作成させてください。
6. **金融・市況分析における4層ストラクチャーと優良ソース中心主義**: 
   - 相場・個別銘柄・投資テーマ調査など金融・市況に関する解説において、文章構成を必ず **【🎯 結論】【🟢 いいニュース・好材料】【🔴 悪いニュース・懸念材料】【🧠 総括】** の順に整理して出力させること。
   - 情報源は企業IR・公的統計などの一次ソースや、大手優良メディア（Reuters, Bloomberg, WSJ, CNBC, 日経新聞等）を中心に厳選し、個人ブログや質の低い二次記事に依拠しないこと。
   - ※【重要：一般トレンドや非金融質問への強制適用禁止】「最近のトレンド」「カルチャー・社会動向」「一般的なニュース」等を尋ねられた際、金融投資のような「好材料/悪材料（🟢/🔴）」や経済一辺倒に縛らず、カルチャー・テック・社会・ライフスタイル等の多様なトピックに沿った自然な構成と見出しで整理させること。また、株価・銘柄と関係ない回答末尾に Yahoo Finance のリンクを追加させてはなりません。
7. **時系列と時制の厳格照合（過去イベントの現在形・未来形誤報禁止）**:
   - ニュースや直近のトレンドを整理する際、システムから注入されている【現在の日時】と記事内のイベント開催日時を比較照合すること。
   - 現在時刻から見て既に終了した過去イベント（例：過去の月に開催済みの大会など）を、「〜に向けて」「今夏のトレンド」等と進行形・未来形のように語ることは重大な時系列誤認です。過去の出来事はトレンド予測から除外するか過去形（「〇月に開催された〜」）で正確に記述させること。"""

# Remove sections from base prompt
base_content = content.replace(coding_section, "")
base_content = base_content.replace(coding_section2, "")
base_content = base_content.replace(travel_section, "")
base_content = base_content.replace(travel_section2, "")
base_content = base_content.replace(finance_section, "")

# Cleanup double blank lines
import re
base_content = re.sub(r'\n{3,}', '\n\n', base_content)

# Write files
with open(os.path.join(base_dir, 'supervisor_prompt.md'), 'w', encoding='utf-8') as f:
    f.write(base_content.strip())

with open(os.path.join(base_dir, 'supervisor_prompt_coding.md'), 'w', encoding='utf-8') as f:
    f.write((coding_section + "\n\n" + coding_section2).strip())

with open(os.path.join(base_dir, 'supervisor_prompt_travel.md'), 'w', encoding='utf-8') as f:
    f.write((travel_section + "\n\n" + travel_section2).strip())

with open(os.path.join(base_dir, 'supervisor_prompt_finance.md'), 'w', encoding='utf-8') as f:
    f.write(finance_section.strip())

print("Prompt split completed.")
